# Task 3 — Dashboard loading shell plan

**Spec source:** `.superpowers/sdd/task-3-brief.md`

**Goal:** Keep a visible, accessible editor shell on `/` until session initialization resolves, then display the editor only when a library exists.

## Scope and constraints

- Consume the Task 2 `initializationStatus` state without changing initialization code.
- Touch only the Task 3 dashboard, dashboard SCSS, four client locale files, and its Playwright coverage, plus required task records.
- Use Vue options API, existing i18n keys, Sass variables, and no dependencies.
- Preserve the existing global error alert/redirect path.

## Testable steps

- [ ] Add a focused Playwright test that delays `POST /signin` and requires `.lpEditorLoading` to be visible before the request completes.
- [ ] Run that file against Chromium and capture the expected missing-shell failure (RED).
- [ ] Add the loading template branch, `isInitializing` computed state, and an immediate initialization-status watcher that either reveals a loaded library or redirects only after `ready` has no library.
- [ ] Add `dash.loadingEditor` to English, French, German, and Spanish; style a stable full-viewport shell with a sidebar and spinner placeholder.
- [ ] Re-run the focused test (GREEN), run `npm run build`, inspect the scoped diff, record evidence, and commit the task.

## Acceptance checks

- A delayed `/signin` response leaves `.lpEditorLoading` visible on the dashboard.
- No eager dashboard redirect occurs while status is `loading`.
- `ready` plus a library reveals `#main`; `ready` without a library navigates to `/welcome`.
- Initialization errors remain owned by the existing global handler.

## Recovery execution constraints — 2026-07-26

- Do not launch Playwright, a development server, or any persistent process.
- Verify the focused test source statically and run `npm run build` as the only runtime verification.
- Remove `.codex-tmp` if present; it is not part of Task 3.
- Commit only Task 3 implementation, test, and required task records.
